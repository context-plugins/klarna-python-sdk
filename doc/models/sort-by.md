
# Sort By

## Enumeration

`SortBy`

## Fields

| Name |
|  --- |
| `CREATED_AT` |
| `ENUM_CREATED_AT` |
| `CLOSED_AT` |
| `ENUM_CLOSED_AT` |

## Example

```python
from klarna.models.sort_by import SortBy

sort_by = SortBy.CLOSED_AT
```

