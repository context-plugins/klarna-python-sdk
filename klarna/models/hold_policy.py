"""klarna.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class HoldPolicy(object):
    """Implementation of the 'HoldPolicy' enum.

    Defines when Klarna debits the Partner for any chargebacks and fees related to
    the dispute.
    * `NONE` - Klarna debits the Partner when the dispute is closed, where relevant.
    * `ON_DISPUTE_INITIATED` - Klarna holds the chargeback and fee amount when the
    dispute moves into `INITIATED` state, and releases it again if the Partner wins
    the dispute.

    Attributes:
        NONE: The enum member of type str.
        ON_DISPUTE_INITIATED: The enum member of type str.

    """

    _all_values = ["NONE", "ON_DISPUTE_INITIATED"]
    NONE = "NONE"

    ON_DISPUTE_INITIATED = "ON_DISPUTE_INITIATED"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
