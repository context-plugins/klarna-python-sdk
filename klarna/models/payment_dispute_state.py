"""klarna.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class PaymentDisputeState(object):
    """Implementation of the 'PaymentDisputeState' enum.

    The current state of the Dispute.
    * `INITIATED` - Dispute initiated. Klarna reviews case details and may request
    Partner response within deadline.
    * `REPRESENTMENT` - Evidence review phase. Partner has submitted evidence and
    Klarna is reviewing the representment case.
    * `PRE_ARBITRATION` - Pre-arbitration phase. Preliminary decision made. Partner
    has opportunity to accept or challenge the preliminary decision.
    * `ARBITRATION` - Arbitration phase. Final review by Klarna. Binding decision
    will be made.
    * `CLOSED` - Dispute is closed. Check `dispute_outcome` and
    `dispute_outcome_detailed` for further information.

    Attributes:
        INITIATED: The enum member of type str.
        REPRESENTMENT: The enum member of type str.
        PRE_ARBITRATION: The enum member of type str.
        ARBITRATION: The enum member of type str.
        CLOSED: The enum member of type str.

    """

    _all_values = ["INITIATED", "REPRESENTMENT", "PRE_ARBITRATION", "ARBITRATION", "CLOSED"]
    INITIATED = "INITIATED"

    REPRESENTMENT = "REPRESENTMENT"

    PRE_ARBITRATION = "PRE_ARBITRATION"

    ARBITRATION = "ARBITRATION"

    CLOSED = "CLOSED"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
