"""klarna.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from klarna.api_helper import APIHelper
from klarna.models.totals import Totals


class Payout(object):
    """Implementation of the 'Payout' model.

    Attributes:
        totals (Totals): The model property of type Totals.
        payment_reference (str): The reference id of the payout
        payout_date (datetime): ISO 8601 formatted date-time string
        currency_code (str): ISO 4217 Currency Code. Like USD, EUR, AUD or GBP.
        currency_code_of_registration_country (str): ISO 4217 Currency Code of the
            country you are registered in.
        merchant_settlement_type (MerchantSettlementType): Whether the amounts are
            net or gross
        merchant_id (str): The merchant id
        transactions (str): Link to the transactions that are part of this payout

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "totals": "totals",
        "payment_reference": "payment_reference",
        "payout_date": "payout_date",
        "currency_code": "currency_code",
        "merchant_settlement_type": "merchant_settlement_type",
        "merchant_id": "merchant_id",
        "currency_code_of_registration_country":
            "currency_code_of_registration_country",
        "transactions": "transactions",
    }

    _optionals = [
        "currency_code_of_registration_country",
        "transactions",
    ]

    def __init__(
        self,
        totals=None,
        payment_reference=None,
        payout_date=None,
        currency_code=None,
        merchant_settlement_type=None,
        merchant_id=None,
        currency_code_of_registration_country=APIHelper.SKIP,
        transactions=APIHelper.SKIP):
        """Initialize a Payout instance."""
        # Initialize members of the class
        self.totals = totals
        self.payment_reference = payment_reference
        self.payout_date =\
             APIHelper.apply_datetime_converter(
            payout_date, APIHelper.RFC3339DateTime)\
             if payout_date else None
        self.currency_code = currency_code
        if currency_code_of_registration_country is not APIHelper.SKIP:
            self.currency_code_of_registration_country =\
                 currency_code_of_registration_country
        self.merchant_settlement_type = merchant_settlement_type
        self.merchant_id = merchant_id
        if transactions is not APIHelper.SKIP:
            self.transactions = transactions

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        totals =\
            Totals.from_dictionary(
                dictionary.get("totals"))\
                if dictionary.get("totals") else None
        payment_reference =\
            dictionary.get("payment_reference")\
            if dictionary.get("payment_reference")\
                else None
        payout_date = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("payout_date")).datetime\
            if dictionary.get("payout_date") else None
        currency_code =\
            dictionary.get("currency_code")\
            if dictionary.get("currency_code")\
                else None
        merchant_settlement_type =\
            dictionary.get("merchant_settlement_type")\
            if dictionary.get("merchant_settlement_type")\
                else None
        merchant_id =\
            dictionary.get("merchant_id")\
            if dictionary.get("merchant_id")\
                else None
        currency_code_of_registration_country =\
            dictionary.get("currency_code_of_registration_country")\
            if dictionary.get("currency_code_of_registration_country")\
                else APIHelper.SKIP
        transactions =\
            dictionary.get("transactions")\
            if dictionary.get("transactions")\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(totals,
                   payment_reference,
                   payout_date,
                   currency_code,
                   merchant_settlement_type,
                   merchant_id,
                   currency_code_of_registration_country,
                   transactions)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _totals=self.totals
        _payment_reference=self.payment_reference
        _payout_date=self.payout_date
        _currency_code=self.currency_code
        _currency_code_of_registration_country=(
            self.currency_code_of_registration_country
            if hasattr(self, "currency_code_of_registration_country")
            else None
        )
        _merchant_settlement_type=self.merchant_settlement_type
        _merchant_id=self.merchant_id
        _transactions=(
            self.transactions
            if hasattr(self, "transactions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"totals={_totals!r}, "
            f"payment_reference={_payment_reference!r}, "
            f"payout_date={_payout_date!r}, "
            f"currency_code={_currency_code!r}, "
            f"currency_code_of_registration_country={_currency_code_of_registration_country!r}, "
            f"merchant_settlement_type={_merchant_settlement_type!r}, "
            f"merchant_id={_merchant_id!r}, "
            f"transactions={_transactions!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _totals=self.totals
        _payment_reference=self.payment_reference
        _payout_date=self.payout_date
        _currency_code=self.currency_code
        _currency_code_of_registration_country=(
            self.currency_code_of_registration_country
            if hasattr(self, "currency_code_of_registration_country")
            else None
        )
        _merchant_settlement_type=self.merchant_settlement_type
        _merchant_id=self.merchant_id
        _transactions=(
            self.transactions
            if hasattr(self, "transactions")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"totals={_totals!s}, "
            f"payment_reference={_payment_reference!s}, "
            f"payout_date={_payout_date!s}, "
            f"currency_code={_currency_code!s}, "
            f"currency_code_of_registration_country={_currency_code_of_registration_country!s}, "
            f"merchant_settlement_type={_merchant_settlement_type!s}, "
            f"merchant_id={_merchant_id!s}, "
            f"transactions={_transactions!s}, "
            f")"
        )
