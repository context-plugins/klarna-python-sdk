"""klarna.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from klarna.api_helper import APIHelper
from klarna.models.base_framework import BaseFramework
from klarna.models.options import Options


class PaymentDisputeConfiguration(object):
    """Implementation of the 'PaymentDisputeConfiguration' model.

    Configuration block that helps partners route dispute handling logic.
    base_framework identifies the dispute framework: FRAMEWORK_2026 for disputes
    routed through the V4 flow, FRAMEWORK_2020 for disputes created through the old
    disputes flow.

    Attributes:
        base_framework (BaseFramework): Dispute framework version.
        options (Options): Additional configuration options for the dispute.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "base_framework": "base_framework",
        "options": "options",
    }

    _optionals = [
        "options",
    ]

    def __init__(
        self,
        base_framework=None,
        options=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a PaymentDisputeConfiguration instance."""
        # Initialize members of the class
        self.base_framework = base_framework
        if options is not APIHelper.SKIP:
            self.options = options

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        base_framework =\
            dictionary.get("base_framework")\
            if dictionary.get("base_framework")\
                else None
        options =\
            Options.from_dictionary(
                dictionary.get("options"))\
                if "options" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(base_framework,
                   options,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.base_framework,
                    type_callable=lambda value:
                        BaseFramework.validate(value))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("base_framework"),
                type_callable=lambda value:
                    BaseFramework.validate(value))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _base_framework=self.base_framework
        _options=(
            self.options
            if hasattr(self, "options")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"base_framework={_base_framework!r}, "
            f"options={_options!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _base_framework=self.base_framework
        _options=(
            self.options
            if hasattr(self, "options")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"base_framework={_base_framework!s}, "
            f"options={_options!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
