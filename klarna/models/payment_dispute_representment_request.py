"""klarna.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from klarna.api_helper import APIHelper


class PaymentDisputeRepresentmentRequest(object):
    """Implementation of the 'PaymentDisputeRepresentmentRequest' model.

    Request for information towards the associated partner

    Attributes:
        additional_information (str): Free-text string with additional information
            about the dispute request
        requested_at (datetime): Timestamp of when the representment request was
            created

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "additional_information": "additional_information",
        "requested_at": "requested_at",
    }

    def __init__(
        self,
        additional_information=None,
        requested_at=None):
        """Initialize a PaymentDisputeRepresentmentRequest instance."""
        # Initialize members of the class
        self.additional_information = additional_information
        self.requested_at =\
             APIHelper.apply_datetime_converter(
            requested_at, APIHelper.RFC3339DateTime)\
             if requested_at else None

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        additional_information =\
            dictionary.get("additional_information")\
            if dictionary.get("additional_information")\
                else None
        requested_at = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("requested_at")).datetime\
            if dictionary.get("requested_at") else None

        # Return an object of this model
        return cls(additional_information,
                   requested_at)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.additional_information,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.requested_at,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        APIHelper.RFC3339DateTime,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("additional_information"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("requested_at"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _additional_information=self.additional_information
        _requested_at=self.requested_at
        return (
            f"{self.__class__.__name__}("
            f"additional_information={_additional_information!r}, "
            f"requested_at={_requested_at!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _additional_information=self.additional_information
        _requested_at=self.requested_at
        return (
            f"{self.__class__.__name__}("
            f"additional_information={_additional_information!s}, "
            f"requested_at={_requested_at!s}, "
            f")"
        )
